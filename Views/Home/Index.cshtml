@model IEnumerable<Employee>




<div class="row">
			@foreach (var employee in Model)
			{
		var photoPath = "~/images/" + (employee.PhotoPath ?? "noimage.png");
			<div class="card" style="min-width: 18rem;max-width:25.5%;">
			<img src="@photoPath" class="card-img-top imageThumbnail" alt="..." asp-append-version="true">
				<ul class="list-group list-group-flush"> n
					<li class="list-group-item">ID :@employee.Id</li>

					<li class="list-group-item">Name :@employee.Name</li>

					<li class="list-group-item">Department : @employee.Department</li>
				</ul>
				<div class="card-body">
				<a asp-controller="home" asp-action="details" asp-route-id="@employee.Id" class="btn btn-primary">show</a>
				@* <a href="@Url.Action("details","home",new {id=employee.Id})" 
				class="btn btn-primary">show</a>
				    @Html.ActionLink("View","details","home",new {id=employee.Id})
				<a href="home/Details/@employee.Id" class="card-link">View</a> *@
					<a href="#" class="card-link">Delete</a>
				</div>
			</div>
			}
		

</div>
